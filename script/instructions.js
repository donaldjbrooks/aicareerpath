// Minimal script for instructions page (just handles the back button, which is already in HTML)
document.addEventListener('DOMContentLoaded', () => {
    // No additional initialization needed for instructions page
});